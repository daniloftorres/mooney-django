# mooney/routes/urls.py
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    # path('admin/', admin.site.urls),
    # path('api/', include('mooney.routes.urls_api')),
    # Outras rotas necessárias
]
